#ifndef COLORS_H
#define COLORS_H
#include <SFML/Graphics/Color.hpp>

namespace Colors
{
	const sf::Color ringColor = sf::Color(196, 188, 0);
	const sf::Color textLitColor = sf::Color(255, 15, 15);
}

#endif